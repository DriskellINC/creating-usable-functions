// function sayHello () {
//     console.log(`Hello!`);
// }
// sayHello() //function declaration

// function expression
// const sayHola = function (name) {
//     console.log ("hola" + name + "!");
// }
// sayHola("Ishiah") // notes

const sum = [sumNumber 
 avg = (sumNumber/avgStringsLongerThan)

const stringsLongerThan(['say', 'hello', 'in', 'the', 'morning']

function sumArrNums(arr) {
    let count = 0;
    arr.forEach(number => count += number());
    return count;
}

/**  operator is doing nothing when the element is a string, you can use Number (or +) to convert elements to numbers (strings will be converted, and numbers will remain numbers, so there is no need for type checking */

// Function to calculate the average of numbers in an array
function averageArray(arr) {

    const sum = sumArray(arr);      //sum of the array
    // Dividing the sum by the length of the array to get the average
    const average = sum / arr.length;
    return average;
}

//to print n
function print(n) {
    if (n === 1) {
        console.log(`n`);
        return
    }
    //recursive process
    print (n - 1);
    console.log(print(n));
}

//   part 2 
const arr[{ id: "42", name: "Bruce", occupation: "Knight", age: "41" },
    { id: "48", name: "Barry", occupation: "Runner", age: "25" },
    { id: "57", name: "Bob", occupation: "Fry Cook", age: "19" },
    { id: "63", name: "Blaine", occupation: "Quiz Master", age: "58" },
    { id: "7", name: "Bilbo", occupation: "None", age: "111" }]

//sort the array by age
sort(function (a, b) { return a.age - b.age; });